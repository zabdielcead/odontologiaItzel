import 'package:flutter/material.dart';

class ServiciosPage extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Mi Servicios'),),
    );
  }
}